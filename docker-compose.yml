version: '3.8'

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs.db:/app/logs.db
      - ./screenshots:/app/screenshots
      - ./memory_db.json:/app/memory_db.json
    environment:
      - HEADLESS=true
      - CDP_URL=
