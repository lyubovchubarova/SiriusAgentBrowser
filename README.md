# Sirius Agent Browser

🤖 **Автономный браузерный агент с Vision Language Model (VLM)**

Sirius Agent Browser — это интеллектуальный агент, способный управлять браузером Google Chrome для выполнения сложных задач. Он использует визуальные модели (VLM) для анализа содержимого веб-страниц и планировщик для построения последовательности действий.

## 🚀 Возможности

-   **Визуальный анализ (VLM)**: Понимает интерфейс сайтов так же, как человек, анализируя скриншоты.
-   **Планирование задач**: Разбивает сложные цели (например, "Найди билеты на самолет") на последовательность простых шагов.
-   **Человекоподобное управление**: Эмулирует движения мыши и ввод текста, чтобы избежать блокировок анти-бот системами.
-   **Расширение для Chrome**: Удобный интерфейс в боковой панели браузера для общения с агентом.
-   **Гибкая архитектура**: Поддержка Yandex Cloud (Gemma 3) и OpenAI (GPT-4o).

## 🏗 Архитектура

Проект состоит из нескольких ключевых компонентов:

-   **Extension (`extension/`)**: Chrome-расширение (Side Panel), которое служит интерфейсом для пользователя и обеспечивает связь с браузером.
-   **Server (`src/server.py`)**: FastAPI сервер, обрабатывающий запросы от расширения и управляющий агентом.
-   **Orchestrator (`src/orchestrator/`)**: Координирует работу планировщика и исполнителя.
-   **Planner (`src/planner/`)**: Генерирует план действий на основе запроса пользователя.
-   **VLM Agent (`src/vlm/`)**: "Глаза" и "Руки" агента. Анализирует скриншоты и выполняет действия (клик, ввод текста).
-   **Browser Controller (`src/browser/`)**: Обертка над Playwright для низкоуровневого управления браузером.

## 🛠 Предварительные требования

-   **OS**: Windows, macOS или Linux.
-   **Python**: 3.10 или выше.
-   **Google Chrome**: Установленный браузер.
-   **API Keys**: Доступ к Yandex Cloud (YandexGPT/Gemma) или OpenAI API.

## ⚙️ Настройка

1.  **Клонируйте репозиторий:**

    ```bash
    git clone https://github.com/lyubovchubarova/SiriusAgentBrowser.git
    cd SiriusAgentBrowser
    ```

2.  **Создайте файл конфигурации:**
    Скопируйте пример конфигурации и заполните его своими ключами:

    ```bash
    cp .env.example .env
    ```

    Отредактируйте `.env`:

    -   `YANDEX_CLOUD_API_KEY` и `YANDEX_CLOUD_FOLDER`: Для использования моделей Yandex (рекомендуется).
    -   `OPENAI_API_KEY`: Если хотите использовать OpenAI.

## 💻 Запуск (Локально)

### 1. Установка зависимостей

Создайте виртуальное окружение и установите пакеты:

**Windows:**

```powershell
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
playwright install
```

**macOS / Linux:**

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
playwright install
```

### 2. Запуск сервера и браузера

Мы подготовили скрипты для быстрого запуска всего необходимого.

**Windows:**
Запустите `start_agent.bat` (двойным кликом или из консоли).
Или через PowerShell:

```powershell
.\scripts\start_all.ps1
```

**macOS / Linux:**

```bash
chmod +x start_agent.sh scripts/*.sh
./start_agent.sh
```

_Что делают эти скрипты:_

1.  Запускают Google Chrome в режиме отладки (порт 9222).
2.  Запускают Python сервер агента (порт 8000).

### 3. Установка расширения

**Автоматически:**
Скрипты запуска (`start_agent.bat` / `start_agent.sh`) пытаются автоматически загрузить расширение в Chrome через флаг `--load-extension`. Если после запуска браузера вы видите иконку Sirius Agent, этот шаг можно пропустить.

**Вручную (если не появилось):**

1.  Откройте Google Chrome.
2.  Перейдите по адресу `chrome://extensions/`.
3.  Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу.
4.  Нажмите **"Загрузить распакованное расширение"** (Load unpacked).
5.  Выберите папку `extension` из корня проекта.

### 4. Использование

1.  Откройте любую веб-страницу.
2.  Нажмите на иконку расширения **Sirius Agent** (или откройте боковую панель Chrome и выберите Sirius Agent).
3.  Дождитесь статуса "Connected to agent server".
4.  Введите задачу, например: _"Найди курс доллара на сегодня"_ или _"Зайди на википедию и найди статью про капибар"_.
5.  Наблюдайте за действиями агента!

## 🐳 Запуск через Docker

Если вы хотите запустить сервер агента в изолированной среде:

1.  Убедитесь, что Docker установлен и запущен.
2.  Запустите контейнер:
    ```bash
    docker-compose up --build
    ```
    _Примечание: Для работы с браузером хоста из Docker контейнера могут потребоваться дополнительные настройки сети или использование headless-браузера внутри контейнера (по умолчанию в docker-compose настроен headless)._

## 🐛 Устранение неполадок

**Проблема: Расширение пишет "Connecting to server..." и не подключается.**

-   Проверьте, запущен ли сервер (в консоли должен быть лог `Uvicorn running on ...`).
-   Убедитесь, что сервер доступен по адресу `http://127.0.0.1:8000/health`.

**Проблема: Агент не может управлять браузером.**

-   Убедитесь, что Chrome запущен с флагом `--remote-debugging-port=9222`. Скрипты запуска делают это автоматически. Если вы запускаете Chrome вручную, закройте все окна Chrome и запустите его командой:
    -   Windows: `chrome.exe --remote-debugging-port=9222`
    -   Mac: `/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222`

**Проблема: Ошибки API ключей.**

-   Проверьте файл `.env`. Убедитесь, что ключи корректны и у вас есть доступ к моделям.

## 👨‍💻 Разработка

Для поддержания качества кода используйте:

```bash
ruff check .        # Линтинг кода
ruff format .       # Форматирование кода
mypy src/           # Проверка типов
```

## 📄 Лицензия

MIT License
